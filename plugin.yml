name: ServerSigns
version: 1.4.0
description: Server Teleport Signs
author: LimeByte

main: com.eyeofender.serversigns.ServerSigns

database: true

commands:
  serversigns:
    description: Various ServerSigns commands

permissions:
  serversigns.use:
    description: Use ServerSigns
    default: true
  serversigns.create:
    description: Create ServerSigns
    default: op
  serversigns.destroy:
    description: Destroy ServerSigns
    default: op
  serversigns.reload:
    description: Reload config from disk
    default: op